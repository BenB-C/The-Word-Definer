<h1>The Word Definer</h1>

<p><em>Click on a word see its definitions.</em></p>

<% if @words.any? %>
  <ul>
    <% @words.each do |word| %>
      <li><a href="/words/<%= word.id %>/definitions"><%= word.text %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are currently no words to display.</p>
<% end %>

<p><a href="/words/new">Add a new word</a></p>
